# Get app mail account (`wechat-work-get-app-mail-account`)

Calls `GET /cgi-bin/mail/get_app_mail_account` to get the application email account.

## Parameters

- **wechat_work_credential** (required): `credential_id` for `wechat-work`.

## Output

Returns the application email account details.