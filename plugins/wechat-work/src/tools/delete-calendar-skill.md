# Delete calendar (`wechat-work-delete-calendar`)

Calls `POST /cgi-bin/calendar/delete` to delete an existing calendar.

## Parameters

- **wechat_work_credential** (required): `credential_id` for `wechat-work`.
- **cal_id** (required): Calendar ID to delete.

## Output

Returns success status on successful deletion.
