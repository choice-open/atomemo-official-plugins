# Delete document (`wechat-work-delete-wedoc`)

Calls `POST /cgi-bin/wedoc/del_doc` to delete a WeDoc document.

## Parameters

- **wechat_work_credential** (required): `credential_id` for `wechat-work`.
- **docid** (required): The document ID to delete.

## Output

Returns success/failure status.
