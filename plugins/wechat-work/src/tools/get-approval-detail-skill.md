# Get approval detail (`wechat-work-get-approval-detail`)

Calls `GET /cgi-bin/oa/get_approval_detail` to retrieve approval details.

## Parameters

- **wechat_work_credential** (required): `credential_id` for `wechat-work`
- **approval_id** (required): Approval application ID

## Output

Returns approval details on success.