# Get application menu (`wechat-work-get-menu`)

Calls `GET /menu/get` to get the custom menu for an application.

## Parameters

- **wechat_work_credential** (required): `credential_id` for `wechat-work`
- **agent_id** (required): Application AgentId

## Output

Returns the menu configuration on success.