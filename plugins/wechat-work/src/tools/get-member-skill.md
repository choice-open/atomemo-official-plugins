# Get member (`wechat-work-get-member`)

Calls `GET /user/get` to retrieve a member's detailed information.

## Parameters

- **wechat_work_credential** (required): `credential_id` for `wechat-work`
- **userid** (required): Member's userid

## Output

Returns member details on success.