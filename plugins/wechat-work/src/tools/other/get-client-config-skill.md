# Get client config (`wechat-work-get-client-config`)

Calls `GET /cgi-bin/mail/client_config/get` to get mail client configuration.

## Parameters

- **wechat_work_credential** (required): `credential_id` for `wechat-work`.

## Output

Returns the client configuration (IMAP/SMTP server settings).