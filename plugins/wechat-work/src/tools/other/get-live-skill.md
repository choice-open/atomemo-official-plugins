# Get live (`wechat-work-get-live`)

Calls `GET /cgi-bin/live/get` to retrieve live broadcast details.

## Parameters

- **wechat_work_credential** (required): `credential_id` for `wechat-work`
- **live_id** (required): Live broadcast ID

## Output

Returns live broadcast details on success.