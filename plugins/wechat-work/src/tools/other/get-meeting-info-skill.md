# Get meeting info (`wechat-work-get-meeting-info`)

Calls `POST /cgi-bin/meeting/get_info` to get meeting details.

## Parameters

- **wechat_work_credential** (required): `credential_id` for `wechat-work`.
- **meeting_id** (required): Meeting ID to get.

## Output

Returns the meeting details.