# Get approval template (`wechat-work-get-approval-template`)

Calls `GET /cgi-bin/oa/get_template` to retrieve approval template details.

## Parameters

- **wechat_work_credential** (required): `credential_id` for `wechat-work`
- **template_id** (required): Approval template ID

## Output

Returns approval template details on success.