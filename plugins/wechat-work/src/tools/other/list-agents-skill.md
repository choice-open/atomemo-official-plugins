# List agents (`wechat-work-list-agents`)

Calls `GET /cgi-bin/agent/list` to get the list of WeChat Work applications.

## Parameters

- **wechat_work_credential** (required): `credential_id` for `wechat-work`.

## Output

Returns the list of applications with their agentid and name.