# Second verify (`wechat-work-second-verify`)

Calls `POST /cgi-bin/user/secondverify` to perform second verification for a member.

## Parameters

- **wechat_work_credential** (required): `credential_id` for `wechat-work`.
- **userid** (required): Userid of the member to verify.

## Output

Returns success status.