# Get external contact (`wechat-work-get-external-contact`)

Calls `GET /externalcontact/get` to get detailed info of an external contact.

## Parameters

- **wechat_work_credential** (required): `credential_id` for `wechat-work`
- **external_userid** (required): External contact's userid

## Output

Returns detailed external contact info on success.