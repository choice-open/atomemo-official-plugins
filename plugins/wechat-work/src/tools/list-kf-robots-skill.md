# List KF robots (`wechat-work-list-kf-robots`)

Calls `GET /cgi-bin/kf/robot/list` to get the list of customer service robots.

## Parameters

- **wechat_work_credential** (required): `credential_id` for `wechat-work`.

## Output

Returns the robot list.